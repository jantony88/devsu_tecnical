#spring.application.name=movimientoService
spring:
  .application:
    name: movimientos_service
    version: 1.0.0
  datasource:
    url: jdbc:postgresql://localhost:5433/db_movimientos #${DATASOURCE_URL}
    username: sonar #${DATASOURCE_USERNAME}
    password: S3cret #${DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: Admin12.
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/